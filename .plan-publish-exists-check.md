# Plan: 发布版本已存在检测（避免重复发布触发限频）

## 问题
重打 tag 触发重复发布时，已存在的版本被当作失败/限频处理，触发 4 次重试（60s/120s/180s 递增），浪费 10+ 分钟并加剧限频。

## 方案：检测"已存在"输出，跳过而非重试

### 1. 加"已存在"检测模式（publish.py）

新增 `_ALREADY_EXISTS` 正则，匹配 ClawHub/SkillHub 返回的"版本已存在"类错误：
```python
_ALREADY_EXISTS = re.compile(
    r"already exists|already published|version.*exists|conflict|duplicate|已存在|已发布|已上传",
    re.IGNORECASE,
)
```

### 2. 在 `_publish_with_retry` 里优先检测"已存在"

在限频重试检查**之前**，加一个"已存在"检查：
```python
# 在 rate_limit 检查之前
if completed.returncode != 0 and _ALREADY_EXISTS.search(output):
    context.out(f"  ⊙ {item} 已发布，跳过")
    return PublishItemResult(target, item, True, "already published, skipped")
```

返回 `ok=True`（成功跳过），不重试、不失败。

### 3. ClawHub 插件包预检（bonus）

`_clawhub_package` 里，发布前用 `clawhub package inspect <name>` 查询 Latest 版本，如果 == 待发布版本，直接跳过：
```python
if not context.dry_run:
    existing = _run_quietly(context, (binary, "package", "inspect", package_name))
    if existing and existing.returncode == 0:
        match = re.search(r"Latest:\s*(\S+)", existing.stdout or "")
        if match and match.group(1) == version:
            context.out(f"  ⊙ {label} 已发布（Latest: {version}），跳过")
            return PublishItemResult("clawhub", "plugin", True, "already published")
```

### 4. 测试

- `test_publish_with_retry_skips_already_published`：模拟 publish 输出含 "already exists"，验证返回 `ok=True` 且不重试
- `test_clawhub_package_skips_when_version_exists`：模拟 inspect 返回 Latest == version，验证跳过

### 文件改动
- `src/skills_eval/publish.py`：加 `_ALREADY_EXISTS` 模式 + `_publish_with_retry` 检测 + `_clawhub_package` 预检
- `tests/test_publish.py`：2 个新测试
